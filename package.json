{
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.2",
    "fs-extra": "^11.2.0",
    "solc": "^0.8.23-fixed"
  },
  "scripts": {
    "compile": "solc --abi --bin --overwrite -o ./build contracts/*.sol"
  }
}
