{
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.2",
    "fs-extra": "^11.2.0",
    "prettier": "^3.1.1",
    "prettier-plugin-solidity": "^1.3.1",
    "solc": "^0.8.23-fixed"
  },
  "scripts": {
    "compile": "solc --abi --bin --overwrite -o ./build contracts/*.sol"
  }
}
